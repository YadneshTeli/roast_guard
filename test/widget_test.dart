import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RoastGuard app launches', (WidgetTester tester) async {
    // Placeholder test — real testing requires native Android platform.
    expect(1 + 1, equals(2));
  });
}
